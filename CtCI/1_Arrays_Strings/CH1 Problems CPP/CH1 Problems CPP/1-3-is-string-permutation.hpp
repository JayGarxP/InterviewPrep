#pragma once
// 1-3-is-string-permutation.hpp
// Question 3 from chapter 1 write function that will return true if one is permutation of other.
// assume permutation just means reordering of sequence and do not count sub strings/partials
// all permutations will have same length/cardinality.
// Also assume all lowercase for now, no whitespace


bool arePermutations(string left, string right);